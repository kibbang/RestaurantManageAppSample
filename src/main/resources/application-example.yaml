spring:
  application:
    name:
  datasource:
    url:
    username:
    password:
    driver-class-name:
  jpa:
    hibernate:
      ddl-auto:
    properties:
      hibernate:
        format_sql:
logging:
  level:
    org.hibernate.SQL: